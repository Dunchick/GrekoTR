{
  "name": "greko-translator",
  "version": "1.0.0",
  "description": "Translator using Netlify Function and RapidAPI",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}